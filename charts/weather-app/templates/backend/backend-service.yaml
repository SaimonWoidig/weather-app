apiVersion: v1
kind: Service
metadata:
  name: weather-app-backend
spec:
  selector:
    app: weather-app-backend
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: http
